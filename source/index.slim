---
title: Ten Dollars
---

.menu-toggle-btn
  span
  span
  span

.navbar
  =link_to 'About', '#about'
  =link_to 'Portfolio', '#portfolio'
  =link_to 'Team', '#team'
  =link_to 'Blog', '#recent-posts'

.overview.fill-page#about
.overview-container.fill-page
  h1 Ten Dollars Studio

  .greet
    p= lorem.sentences 5
    /   | here
    /   br
    /   | we make changes,
    /   br
    /   | we make differences,
    /   br
    /   | we make peace,
    /   br
    /   | we make loves...
    / p we build our own world in a different way
    / p have fun!

.portfolio.wrap-content#portfolio
  .flex-container
    h1 Portfolio
    .flex-row
      .image-container.gold
        .feature-image style="background-image: url('../images/team-20.png');"
        .content
          h3 Ten Dollars
      .image-container
        .feature-image style="background-image: url('../images/oceanic-next.png');"
        .content
          h3 Oceanic Next
    .flex-row
      .image-container.red
        .feature-image style="background-image: url('../images/yshin.png');"
        .content
          h3 Yishin Chou
      .image-container.blue
        .feature-image style="background-image: url('../images/jobs.png');"
        .content
          h3 Steve Jobs

.team.wrap-content#team
  .flex-container
    h1 Team
    .team-flex-flow
      .profile-container
        .head-image style="background-image: url('../images/yukai.jpg');"
        .head-border
      .profile-container
        .head-image style="background-image: url('../images/neson.jpg');"
        .head-border
      .profile-container
        .head-image style="background-image: url('../images/lucas.jpg');"
        .head-border
      .profile-container
        .head-image style="background-image: url('../images/cyuan.jpg');"
        .head-border

.recent-posts.fill-page#recent-posts
.footer#footer

javascript:
  $(function() {
    $('a[href*=#]:not([href=#])').click(function() {
      if (location.pathname.replace(/^\//,'') == this.pathname.replace(/^\//,'') && location.hostname == this.hostname) {
        var target = $(this.hash);
        target = target.length ? target : $('[name=' + this.hash.slice(1) +']');
        if (target.length) {
          $('html,body').animate({
            scrollTop: target.offset().top + 1
          }, 750, $.bez([.55,0,.1,1]));
          return false;
        }
      }
    });
  });
